Cara Commit GIT

0.1 git config --global user.name "Nama"
0.2 git config --global user.email <Email>

Itu buat inisialisasi author (nama dan email)
0.3 Cara cek hasil di atas dengan git status, nanti ada 

1. git init (Buat inisialisasi file agar jadi master)
2. git add <Nama file> / git add . (Kalo banyak)
3. git status (Buat liat status file mana yg dah di add)
4. git commit -m "Pesan" 
5. git log / git log --oneline (Supaya ringkas)

file yang di init akan masuk work three
file yang di add akan masuk staging area 
file yang di commit akan tersimpan di history git

Cara push

ketik : git remote add baru https://github.com/wintonalmundarisna/Latihan-GIT.git
baru adalah nama remotenya, http diambil dari link new repo nya

Jalankan git remote -v untuk cek remote

ketik : git push baru master (Master adalah nama branchnya)
Nanti sign in aja pake browser

Kalo mau mengembalikan file dari taging area ke working three
Ketik : git restore --staged <Nama file>
Lalu commit dan push spt cara di atas

Cara menambahkan versi ketika di cek di git log --oneline
Ketik git log --oneline, ambil kode sebelah kiri file, lalu ketik git tag -a <Mau v berapa, custom> <versi tadi> -m "Pesan"


Cara menambahkan file langsung dari git, ketik add file, lalu masukkan nama file beserta eksistensinya dan isi kodenya, lalu masukkan nama commit di paling bawah dan commit, file tsb belum ada di vscode kita


Cara git pull (Mengunduh kode yang ada di repo github ke com kita)
ketik : git pull <nama remote / si baru tadi> master (Nama branch)


Kalo pas pull ada konflict, buka file yg bersangkutan, yang hijau dari komputer, yang biru dari github 

Cara pilihnya, hapus salah satu